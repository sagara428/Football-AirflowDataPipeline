checks for fact_tournaments:
  - row_count > 0 # check if the table is empty or not
  - schema:
      fail:
        when required column missing: [
          date, home_team, away_team, result, 
          home_team_win, home_team_id, away_team_id          
          ]
        when wrong column type:
          date: date
          home_team: string
          away_team: string
          result: string
          home_team_win: string
          home_team_id: integer
          away_team_id: integer